// Import required  hooks
import { DogFact } from "./components/DogFact.jsx";

export const App = () => {
  // Hint: Initialize state for storing the dog fact
  // Hint: Define the API endpoint
  // Hint: Create a function to fetch the dog fact
  use Effect (() => {
    [dependency])
  })
  // Hint: Use the useEffect hook to fetch the dog fact when the component mounts

  return (
    <div className="App">
       <>
      <h1>Dogfacts and APIs</h1>
      <p>Let's practise calling API's within a Dogfact project</p>
      <P Dogfacts />
    </>
  );
};

export default App;
  );
};
